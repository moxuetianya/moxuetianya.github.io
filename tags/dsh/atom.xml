<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title>墨雪天琊 - dsh</title>
    <subtitle>墨雪天琊的技术博客</subtitle>
    <link rel="self" type="application/atom+xml" href="https://moxuetianya.github.io/tags/dsh/atom.xml"/>
    <link rel="alternate" type="text/html" href="https://moxuetianya.github.io"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-09-13T17:48:42+08:00</updated>
    <id>https://moxuetianya.github.io/tags/dsh/atom.xml</id>
    <entry xml:lang="en">
        <title>让 Agent 调用模型却拿不到 API Key：OpenShell 实测、DSH 默认隔离与 SSH Key 的边界</title>
        <published>2026-09-13T17:48:42+08:00</published>
        <updated>2026-09-13T17:48:42+08:00</updated>
        
        <author>
          <name>
            
              moxuetianya
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://moxuetianya.github.io/2026/09/agent-apikey-isolation-openshell-dsh/"/>
        <id>https://moxuetianya.github.io/2026/09/agent-apikey-isolation-openshell-dsh/</id>
        
        <content type="html" xml:base="https://moxuetianya.github.io/2026/09/agent-apikey-isolation-openshell-dsh/">&lt;h1 id=&quot;bei-jing&quot;&gt;背景&lt;&#x2F;h1&gt;
&lt;p&gt;Agent 一旦有了 shell 工具，&quot;密钥不进它上下文&quot;就变成了一件很难保证的事：&lt;code&gt;env&lt;&#x2F;code&gt;、&lt;code&gt;.env&lt;&#x2F;code&gt;、&lt;code&gt;~&#x2F;.config&#x2F;...&lt;&#x2F;code&gt; 都是它伸手就能读的；prompt injection 还能诱导它把 key 发到外部。目标很明确：&lt;&#x2F;p&gt;
&lt;blockquote&gt;
&lt;p&gt;Hermes &#x2F; DSH &#x2F; 任意 agent 能正常调用模型，但 agent 进程本身&lt;strong&gt;拿不到真实 API Key&lt;&#x2F;strong&gt;。&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;p&gt;围绕这个问题最近做了一轮调研，并拿手头的环境做了实测（dut 上跑着 OpenShell + Hermes 沙箱，见&lt;a href=&quot;https:&#x2F;&#x2F;moxuetianya.github.io&#x2F;2026&#x2F;09&#x2F;openshell-pi-hermes-sandbox&#x2F;&quot;&gt;把 Hermes 关进 OpenShell 沙箱&lt;&#x2F;a&gt;）。顺手把 SSH key、DSH 的默认隔离、以及几个轻量替代方案一起理清。先给结论：&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;API key 防泄漏有两条路线：&lt;strong&gt;把真 key 放在 agent 够不到的地方&lt;&#x2F;strong&gt;（代理网关 &#x2F; 宿主进程），或&lt;strong&gt;让 agent 跑在够不到 key 的沙箱里&lt;&#x2F;strong&gt;（内核隔离）。前者防&quot;读进程&#x2F;文件&quot;，后者防&quot;读宿主文件&quot;。&lt;&#x2F;li&gt;
&lt;li&gt;OpenShell 走的是前者：L7 代理 + 凭证占位符替换，外加文件&#x2F;网络&#x2F;进程&#x2F;凭证四层声明式 policy，dut 上实测可用。但它有一个&quot;占位符进对话体&quot;的坑（§3.4），一旦踩中，会话里所有模型请求都会失败。&lt;&#x2F;li&gt;
&lt;li&gt;SSH key 是完全不同的问题：私钥本来就不传输，防的是&quot;谁能使用签名能力&quot;。OpenShell 目前&lt;strong&gt;没有现成的 ssh-agent 转发&#x2F;挂载&lt;&#x2F;strong&gt;，网上那套 &lt;code&gt;IdentityAgent=$SSH_AUTH_SOCK&lt;&#x2F;code&gt; 的配置在 OpenShell 里配不出来（§4.3）。&lt;&#x2F;li&gt;
&lt;li&gt;如果只想轻量解决&quot;agent 拿不到 API key&quot;：本地代理网关（LiteLLM 之类）把 agent 的 shell 扔进容器&#x2F;独立用户，比 OpenShell 省事得多（§5）。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;h1 id=&quot;yi-xian-fen-qing-san-ceng-ji-zhi&quot;&gt;一、先分清三层机制&lt;&#x2F;h1&gt;
&lt;p&gt;讨论&quot;防泄漏&quot;之前先分清防的是哪一段，否则很容易把三个东西混在一起：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;层&lt;&#x2F;th&gt;&lt;th&gt;代表机制&lt;&#x2F;th&gt;&lt;th&gt;防住什么&lt;&#x2F;th&gt;&lt;th&gt;防不住什么&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;网络&#x2F;应用层代理&lt;&#x2F;td&gt;&lt;td&gt;OpenShell supervisor proxy&lt;&#x2F;td&gt;&lt;td&gt;真 key 不进 agent 进程；出网端点白名单 + L7 审计&lt;&#x2F;td&gt;&lt;td&gt;agent 读宿主文件（靠别的层）&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;内核沙箱&lt;&#x2F;td&gt;&lt;td&gt;bwrap + Landlock + seccomp（DSH、容器）&lt;&#x2F;td&gt;&lt;td&gt;agent 读到宿主敏感文件 &#x2F; 提权&lt;&#x2F;td&gt;&lt;td&gt;同用户下的进程逃逸、&lt;code&gt;&#x2F;proc&#x2F;&amp;lt;pid&amp;gt;&#x2F;environ&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;密码学&lt;&#x2F;td&gt;&lt;td&gt;SSH 公钥认证&lt;&#x2F;td&gt;&lt;td&gt;私钥在网络上被截获&lt;&#x2F;td&gt;&lt;td&gt;私钥被本地进程读走、签名能力被滥用&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;关于 OpenShell 的常见误解：MITM 只是它的&lt;strong&gt;实现手段&lt;&#x2F;strong&gt;（代理终结 TLS 才能替换凭证），不是目的。它真正做的是&quot;凭证占位符 + 策略引擎 + 审计&quot;三件套。&lt;&#x2F;p&gt;
&lt;h1 id=&quot;er-openshell-shi-zen-me-zuo-dao-agent-na-bu-dao-key-de-dut-shi-ce&quot;&gt;二、OpenShell 是怎么做到 agent 拿不到 key 的（dut 实测）&lt;&#x2F;h1&gt;
&lt;h2 id=&quot;2-1-lian-lu&quot;&gt;2.1 链路&lt;&#x2F;h2&gt;
&lt;pre&gt;&lt;code&gt;CLI ──mTLS──▶ openshell-gateway（控制面，127.0.0.1:17670）
                  │  真 key 存在 gateway 的加密凭证库里
                  ▼
          sandbox 容器（rootless podman）
            └─ supervisor（PID 1）
                 ├─ 给 agent 进程注入占位符环境变量
                 ├─ 出网强制走 10.200.0.1:3128 正向代理
                 └─ 对 L7 端点终结 TLS，把占位符替换成真 key 再发上游
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h2 id=&quot;2-2-shi-ce-zheng-ju&quot;&gt;2.2 实测证据&lt;&#x2F;h2&gt;
&lt;p&gt;agent 进程看到的环境变量长这样：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;text&quot; class=&quot;language-text &quot;&gt;&lt;code class=&quot;language-text&quot; data-lang=&quot;text&quot;&gt;$ openshell sandbox exec -n hermes -- sh -c &amp;#x27;env | grep XIAOMI&amp;#x27;
XIAOMI_TOKEN_PLAN_CN_API_KEY=openshell:resolve:env:v..._XIAOMI_TOKEN_PLAN_CN_API_KEY
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;真 key 只在 gateway 的加密库里，代理在发往 &lt;code&gt;token-plan-cn.xiaomimimo.com:443&lt;&#x2F;code&gt; 的请求上替换（临时 CA 的私钥只在 supervisor 内存里）。每次 allow&#x2F;deny 都有 OCSF 日志：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;text&quot; class=&quot;language-text &quot;&gt;&lt;code class=&quot;language-text&quot; data-lang=&quot;text&quot;&gt;$ openshell logs hermes --tail
HTTP:POST [INFO] ALLOWED POST http:&amp;#x2F;&amp;#x2F;token-plan-cn.xiaomimimo.com:443&amp;#x2F;v1&amp;#x2F;chat&amp;#x2F;completions [policy:hermes engine:l7]
NET:OPEN  [MED ] DENIED &amp;#x2F;usr&amp;#x2F;bin&amp;#x2F;curl -&amp;gt; example.com:443 [reason:endpoint not allowed by any policy]
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h2 id=&quot;2-3-policy-guan-shen-me&quot;&gt;2.3 policy 管什么&lt;&#x2F;h2&gt;
&lt;p&gt;OpenShell 的 policy 分静态段（创建时锁定，改了要重建沙箱）和动态段（可热更）：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;字段&lt;&#x2F;th&gt;&lt;th&gt;类别&lt;&#x2F;th&gt;&lt;th&gt;作用&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;filesystem_policy.read_only&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;read_write&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;静态&lt;&#x2F;td&gt;&lt;td&gt;Landlock 白名单，未列出的路径不可访问&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;landlock.compatibility&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;静态&lt;&#x2F;td&gt;&lt;td&gt;&lt;code&gt;best_effort&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;hard_requirement&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;process.run_as_user&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;run_as_group&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;静态&lt;&#x2F;td&gt;&lt;td&gt;agent 降权身份；&lt;code&gt;0&lt;&#x2F;code&gt;&#x2F;root 被 schema 拒绝&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;network_policies.&amp;lt;key&amp;gt;&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;动态&lt;&#x2F;td&gt;&lt;td&gt;端点 × 二进制白名单；&lt;code&gt;protocol: rest&lt;&#x2F;code&gt; = L7 检查&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;request_body_credential_rewrite&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;动态&lt;&#x2F;td&gt;&lt;td&gt;是否重写&lt;strong&gt;请求体&lt;&#x2F;strong&gt;里的占位符（默认 &lt;code&gt;false&lt;&#x2F;code&gt;，见 §3.4）&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;沙箱容器本身也不是&quot;随便挂载&quot;的：dut 上它只挂了 &lt;code&gt;&#x2F;sandbox&lt;&#x2F;code&gt; 卷 + supervisor 二进制 + TLS 文件，宿主 home&#x2F;ssh&#x2F;pass 都不可见（这条很关键，后面 §2.5 会纠一个常见的误传）。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;2-4-keng-zhan-wei-fu-jin-ru-dui-hua-qing-qiu-ti-hui-hua-yong-jiu-shi-bai&quot;&gt;2.4 坑：占位符进入对话&#x2F;请求体 = 会话永久失败&lt;&#x2F;h2&gt;
&lt;p&gt;这是本次实测踩到的新坑，也是写这篇文章的起因。&lt;&#x2F;p&gt;
&lt;p&gt;Hermes 排查问题时用 terminal 工具跑了 &lt;code&gt;env&lt;&#x2F;code&gt;&#x2F;读 &lt;code&gt;.env&lt;&#x2F;code&gt;，把 &lt;code&gt;openshell:resolve:env:...&lt;&#x2F;code&gt; 占位符留在了会话历史里。之后这个会话的&lt;strong&gt;每一次&lt;&#x2F;strong&gt;模型请求体里都带着占位符，而 OpenShell 代理按设计拒绝：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;text&quot; class=&quot;language-text &quot;&gt;&lt;code class=&quot;language-text&quot; data-lang=&quot;text&quot;&gt;HTTP:POST [INFO] ALLOWED POST ...&amp;#x2F;v1&amp;#x2F;chat&amp;#x2F;completions
NET:TRAFFIC [HIGH] DENIED ... [reason:POST request body credential traffic denied]
NET:FAIL ...
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;代理直接掐断连接，客户端报 &lt;code&gt;APIConnectionError: Server disconnected without sending a response&lt;&#x2F;code&gt;，Hermes 显示&quot;model server not responding&quot;——&lt;strong&gt;新会话（&lt;code&gt;hermes -z&lt;&#x2F;code&gt;）正常，只有被污染的会话全挂&lt;&#x2F;strong&gt;。&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;根因：源码 &lt;code&gt;l7&#x2F;rest.rs&lt;&#x2F;code&gt; 的 &lt;code&gt;emit_uninspected_body_credential_denial&lt;&#x2F;code&gt;（finding &lt;code&gt;openshell.credentials.traffic_uninspectable&lt;&#x2F;code&gt;）。请求体里的凭证占位符无法安全检查&#x2F;重写，默认 fail-closed。&lt;&#x2F;li&gt;
&lt;li&gt;修法：把占位符从会话历史里清掉（&lt;code&gt;&#x2F;reset&lt;&#x2F;code&gt; 开新会话，或清理 state.db 后让会话缓存重载）。&lt;&#x2F;li&gt;
&lt;li&gt;别急着开 &lt;code&gt;request_body_credential_rewrite: true&lt;&#x2F;code&gt;：我们的 &lt;code&gt;open_internet&lt;&#x2F;code&gt; 是 hostless（任意主机）端点，开了等于把真 key 替换进发往任意主机的请求体，安全上不划算。&lt;&#x2F;li&gt;
&lt;li&gt;教训：&lt;strong&gt;agent 永远不要读自己的 key&#x2F;占位符&lt;&#x2F;strong&gt;（&lt;code&gt;env | grep&lt;&#x2F;code&gt;、&lt;code&gt;cat .env&lt;&#x2F;code&gt;）。对 agent 来说，那段字符串看起来&quot;不是 secret&quot;，但它会让整个会话失去与外界的模型通信。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;h2 id=&quot;2-5-kan-wu-ji-ge-wang-shang-liu-chuan-de-openshell-shuo-fa&quot;&gt;2.5 勘误：几个网上流传的 OpenShell 说法&lt;&#x2F;h2&gt;
&lt;p&gt;调研时拿到的二手资料里有几处张冠李戴，照着配会踩空：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;流传说法&lt;&#x2F;th&gt;&lt;th&gt;事实&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;&quot;&lt;code&gt;workspaceAccess: ro&#x2F;rw&lt;&#x2F;code&gt; 会把宿主工作目录挂进容器&quot;&lt;&#x2F;td&gt;&lt;td&gt;这是 &lt;strong&gt;OpenClaw&lt;&#x2F;strong&gt; 的配置（&lt;code&gt;~&#x2F;.openclaw&#x2F;openclaw.json&lt;&#x2F;code&gt;），不是 NVIDIA OpenShell。OpenShell 沙箱只挂 &lt;code&gt;&#x2F;sandbox&lt;&#x2F;code&gt; 卷和 supervisor&#x2F;TLS 文件，不挂宿主工作目录&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&quot;&lt;code&gt;tools.exec.host: gateway&lt;&#x2F;code&gt; 时命令直接在宿主机执行、容器内 uid=0 就是宿主 root&quot;&lt;&#x2F;td&gt;&lt;td&gt;同样是别家的说法。OpenShell 的 agent 进程在沙箱内降权运行，&lt;code&gt;uid=0&lt;&#x2F;code&gt;&#x2F;root 被 policy schema 和 supervisor 双重拒绝&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&quot;未列出的端点会弹窗让你实时审批&quot;&lt;&#x2F;td&gt;&lt;td&gt;OpenShell 是声明式 deny + OCSF 日志，没有交互审批&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&quot;审计日志不可篡改&quot;&lt;&#x2F;td&gt;&lt;td&gt;OCSF 事件流可以看 allow&#x2F;deny，但&quot;不可篡改&quot;是营销词，别当合规保证&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;h1 id=&quot;san-ssh-key-he-api-key-shi-tong-yi-ge-wen-ti-ma&quot;&gt;三、SSH key：和 API key 是同一个问题吗？&lt;&#x2F;h1&gt;
&lt;p&gt;不是。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;3-1-ssh-si-yao-ben-lai-jiu-bu-chuan-shu&quot;&gt;3.1 SSH 私钥本来就不传输&lt;&#x2F;h2&gt;
&lt;p&gt;SSH 公钥认证里，私钥永远留在本地，网络上传的是&quot;用私钥对握手数据（含会话 ID）的签名&quot;。中间人即使截获签名，也无法转发给真实服务器——因为它和客户端、服务器各自建了不同的连接，会话 ID 对不上，签名验证必然失败。&lt;strong&gt;所以 SSH key 的泄漏模型不是&quot;传输被截&quot;，而是：私钥文件被读走，或者签名能力被滥用。&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;h2 id=&quot;3-2-agent-forwarding-shi-shuang-ren-jian&quot;&gt;3.2 agent forwarding 是双刃剑&lt;&#x2F;h2&gt;
&lt;p&gt;&lt;code&gt;ssh -A&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;ForwardAgent yes&lt;&#x2F;code&gt; 会把本地的 ssh-agent 通道借给远端：远端的进程可以请求你的 agent 签名。目标服务器一旦被攻破，攻击者就能用你的身份去连别的机器。&lt;strong&gt;除非明确需要跳板链，否则不要开。&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;h2 id=&quot;3-3-zai-openshell-li-rang-agent-yong-ssh-dan-na-bu-dao-si-yao&quot;&gt;3.3 在 OpenShell 里让 agent 用 SSH 但拿不到私钥？&lt;&#x2F;h2&gt;
&lt;p&gt;现状要说清楚：&lt;strong&gt;OpenShell 没有现成的 ssh-agent 转发&#x2F;挂载开关&lt;&#x2F;strong&gt;。沙箱容器的挂载由 driver 决定（我们这份配置只有 &lt;code&gt;&#x2F;sandbox&lt;&#x2F;code&gt; 卷 + TLS 文件），你没法把宿主的 &lt;code&gt;$SSH_AUTH_SOCK&lt;&#x2F;code&gt; 塞进去；网络默认也只放行 80&#x2F;443，22 端口需要显式加。所以网上那套&quot;宿主机跑 ssh-agent + 沙箱里 &lt;code&gt;ssh -o IdentityAgent=$SSH_AUTH_SOCK&lt;&#x2F;code&gt;&quot;在 OpenShell 里是配不出来的，除非改 driver &#x2F; 做 BYOC 深度定制。&lt;&#x2F;p&gt;
&lt;p&gt;可行的路线，按推荐程度排：&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;短时 SSH 证书&lt;&#x2F;strong&gt;（step-ca &#x2F; Vault &#x2F; Teleport 之类的 SSH CA）：给 agent 发分钟级&#x2F;小时级证书。证书在沙箱里，但泄漏窗口极小、可撤销、可绑定主体。这是&quot;让 agent 能 SSH&quot;最稳的做法。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;宿主侧受控执行器&lt;&#x2F;strong&gt;：私钥和 agent 留在宿主机，把&quot;固定几条 ssh 命令&quot;包成一个受控服务&#x2F;脚本，通过 policy 放行的端口给沙箱调用。agent 只有&quot;请求执行&quot;的能力，没有 key。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;专用受限 key 放沙箱&lt;&#x2F;strong&gt;：用远端 &lt;code&gt;authorized_keys&lt;&#x2F;code&gt; 的 &lt;code&gt;command=&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;from=&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;no-pty&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;no-port-forwarding&lt;&#x2F;code&gt; 约束死。实现最简单，但等于放弃&quot;agent 看不到 key&quot;，只适合低价值目标。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;不让沙箱做 SSH&lt;&#x2F;strong&gt;：这类操作留在宿主或 CI，agent 只要结果。&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;h1 id=&quot;si-dsh-deepseek-harness-de-mo-ren-ge-chi&quot;&gt;四、DSH（DeepSeek Harness）的默认隔离&lt;&#x2F;h1&gt;
&lt;blockquote&gt;
&lt;p&gt;这一节主要整理自公开资料，&lt;strong&gt;未在本机逐项实测&lt;&#x2F;strong&gt;；本机的 DSH 部署形态见&lt;a href=&quot;https:&#x2F;&#x2F;moxuetianya.github.io&#x2F;2026&#x2F;08&#x2F;caddy-reverse-proxy-dsh-web-lan&#x2F;&quot;&gt;caddy 反代 DSH Web&lt;&#x2F;a&gt;。配置前建议以实际版本的文档&#x2F;行为为准。&lt;&#x2F;p&gt;
&lt;&#x2F;blockquote&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;内核级沙箱&lt;&#x2F;strong&gt;：Linux 用 bwrap（namespace 拼文件系统视图）+ Landlock（内核强制访问控制）；macOS 用 Seatbelt；Windows 用 ACL 受限令牌；隔离不可用时 fail-closed（宁可拒绝执行，不悄悄降级）。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;默认 workspace-write&lt;&#x2F;strong&gt;：agent（含 shell 工具）在沙箱内运行，只能在工作区目录内写；工作区之外按只读视图挂载，读得到代码&#x2F;调试工具，改不了系统文件。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;凭据只写不回读&lt;&#x2F;strong&gt;：Web UI 里填的 API key 写入 &lt;code&gt;$DSH_HOME&#x2F;.credentials.yaml&lt;&#x2F;code&gt;，前端只显示脱敏描述符。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;默认只监听回环&lt;&#x2F;strong&gt;：&lt;code&gt;127.0.0.1&lt;&#x2F;code&gt;，CLI 默认拒绝 &lt;code&gt;--host 0.0.0.0&lt;&#x2F;code&gt;（我们这台就是 caddy 反代 + basicauth 暴露，配置平面另有 loopback 围栏）。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;边界&lt;&#x2F;strong&gt;：&lt;code&gt;.credentials.yaml&lt;&#x2F;code&gt; 是同用户 600 文件，&lt;strong&gt;同一 OS 用户下的其他进程理论上仍可读&lt;&#x2F;strong&gt;（以及 &lt;code&gt;&#x2F;proc&#x2F;&amp;lt;pid&amp;gt;&#x2F;environ&lt;&#x2F;code&gt;）。如果威胁模型包含&quot;agent 逃逸出沙箱后以同用户身份活动&quot;，需要把 DSH 跑在独立用户或容器里。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;h1 id=&quot;wu-qing-liang-fang-an-dui-bi&quot;&gt;五、轻量方案对比&lt;&#x2F;h1&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;方案&lt;&#x2F;th&gt;&lt;th&gt;复杂度&lt;&#x2F;th&gt;&lt;th&gt;agent 拿得到真 key？&lt;&#x2F;th&gt;&lt;th&gt;适合场景&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;环境变量 &#x2F; &lt;code&gt;.env&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;最低&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;能&lt;&#x2F;strong&gt;（同用户进程可读 &lt;code&gt;.env&lt;&#x2F;code&gt;、&lt;code&gt;&#x2F;proc&#x2F;&amp;lt;pid&amp;gt;&#x2F;environ&lt;&#x2F;code&gt;）&lt;&#x2F;td&gt;&lt;td&gt;只防误提交，不防 agent&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;独立 OS 用户跑 agent&lt;&#x2F;td&gt;&lt;td&gt;低&lt;&#x2F;td&gt;&lt;td&gt;跨用户读不到；同用户逃逸才行&lt;&#x2F;td&gt;&lt;td&gt;单机多用户、轻量加固&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;agent 的 shell 进容器&lt;&#x2F;td&gt;&lt;td&gt;中&lt;&#x2F;td&gt;&lt;td&gt;拿不到（key 留在宿主进程）&lt;&#x2F;td&gt;&lt;td&gt;个人日常，性价比高&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;本地代理网关（LiteLLM 等）&lt;&#x2F;td&gt;&lt;td&gt;中&lt;&#x2F;td&gt;&lt;td&gt;拿不到（agent 只有 fake token，真 key 在网关进程）&lt;&#x2F;td&gt;&lt;td&gt;多 key&#x2F;多模型统一管理&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;OpenShell&lt;&#x2F;td&gt;&lt;td&gt;高&lt;&#x2F;td&gt;&lt;td&gt;拿不到，且带出网&#x2F;文件&#x2F;审计策略&lt;&#x2F;td&gt;&lt;td&gt;需要完整边界与可审计&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;DSH 内置沙箱&lt;&#x2F;td&gt;&lt;td&gt;低（对 DSH 而言）&lt;&#x2F;td&gt;&lt;td&gt;取决于 agent 形态；同用户边界仍在&lt;&#x2F;td&gt;&lt;td&gt;本来就用 DSH&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;一个容易忽略的点：&lt;strong&gt;&quot;独立 OS 用户&quot;防的是跨用户读取，不防同用户逃逸&lt;&#x2F;strong&gt;；&quot;本地代理网关&quot;防的是 key 泄漏，不防 agent 破坏宿主文件。选方案前先写清楚威胁模型。&lt;&#x2F;p&gt;
&lt;h1 id=&quot;liu-zhao-zhao-pei-zhi-de-qing-dan&quot;&gt;六、照着配置的清单&lt;&#x2F;h1&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;OpenShell &#x2F; Hermes（已有环境）&lt;&#x2F;strong&gt;
&lt;ul&gt;
&lt;li&gt;验证占位符：&lt;code&gt;openshell sandbox exec -n hermes -- sh -c &#x27;env | grep XIAOMI&#x27;&lt;&#x2F;code&gt;，应该只看到 &lt;code&gt;openshell:resolve:env:...&lt;&#x2F;code&gt;。&lt;&#x2F;li&gt;
&lt;li&gt;纪律：agent 不允许 &lt;code&gt;env | grep&lt;&#x2F;code&gt;、&lt;code&gt;cat .env&lt;&#x2F;code&gt;（占位符进上下文会让会话永久失败，§2.4）。&lt;&#x2F;li&gt;
&lt;li&gt;cron 任务注意 provider 快照：全局换 provider 后，任务默认仍钉在创建时的快照上，需要 &lt;code&gt;cron.model_provider&lt;&#x2F;code&gt; 或 &lt;code&gt;hermes cron edit --provider&lt;&#x2F;code&gt;。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;宿主 DSH&lt;&#x2F;strong&gt;
&lt;ul&gt;
&lt;li&gt;确认默认沙箱生效（workspace-write）；&lt;code&gt;.credentials.yaml&lt;&#x2F;code&gt; 权限 600；威胁模型包含同用户逃逸就换独立用户&#x2F;容器。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;SSH&lt;&#x2F;strong&gt;
&lt;ul&gt;
&lt;li&gt;绝不开 &lt;code&gt;ForwardAgent&lt;&#x2F;code&gt;；优先走 SSH CA 短证书或宿主侧受控执行器；不要照搬&quot;挂 &lt;code&gt;SSH_AUTH_SOCK&lt;&#x2F;code&gt;&quot;的说法（OpenShell 挂不进去）。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;验收命令&lt;&#x2F;strong&gt;
&lt;ul&gt;
&lt;li&gt;OpenShell：&lt;code&gt;openshell logs &amp;lt;sandbox&amp;gt; --tail&lt;&#x2F;code&gt; 看 allow&#x2F;deny；&lt;code&gt;env&lt;&#x2F;code&gt; 看占位符。&lt;&#x2F;li&gt;
&lt;li&gt;DSH：&lt;code&gt;ls -l $DSH_HOME&#x2F;.credentials.yaml&lt;&#x2F;code&gt;；在 agent 里尝试 &lt;code&gt;cat ~&#x2F;.dsh&#x2F;.credentials.yaml&lt;&#x2F;code&gt; 应被沙箱拒绝。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;h1 id=&quot;zong-jie&quot;&gt;总结&lt;&#x2F;h1&gt;
&lt;ul&gt;
&lt;li&gt;API key 防泄漏的关键不是&quot;藏起来&quot;，而是&lt;strong&gt;让真值不进入 agent 进程&lt;&#x2F;strong&gt;：要么代理网关侧替换（OpenShell、LiteLLM），要么把 agent 关进内核沙箱（DSH、bwrap+Landlock）。&lt;&#x2F;li&gt;
&lt;li&gt;OpenShell 的能力是&quot;凭证注入 + 策略 + 审计&quot;三合一，实测有效，但要接受它的复杂度，并注意占位符不能进入对话体这个坑。&lt;&#x2F;li&gt;
&lt;li&gt;SSH key 是密码学层面的问题，防的是签名能力滥用而非传输截获；想在沙箱里&quot;用 SSH 但看不到私钥&quot;，OpenShell 目前没有现成方案，优先考虑 SSH CA 短证书或宿主侧受控执行器。&lt;&#x2F;li&gt;
&lt;li&gt;二手资料里&quot;OpenShell 挂宿主工作目录 &#x2F; 命令在宿主执行&quot;之类的说法来自别家产品（OpenClaw），照着配会踩空——以实测和官方 policy schema 为准。&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
</content>
        
    </entry>
    <entry xml:lang="en">
        <title>caddy 反代 dsh web，让局域网用户也能访问</title>
        <published>2026-08-24T14:37:02+08:00</published>
        <updated>2026-08-24T14:37:02+08:00</updated>
        
        <author>
          <name>
            
              moxuetianya
            
          </name>
        </author>
        
        <link rel="alternate" type="text/html" href="https://moxuetianya.github.io/2026/08/caddy-reverse-proxy-dsh-web-lan/"/>
        <id>https://moxuetianya.github.io/2026/08/caddy-reverse-proxy-dsh-web-lan/</id>
        
        <content type="html" xml:base="https://moxuetianya.github.io/2026/08/caddy-reverse-proxy-dsh-web-lan/">&lt;p&gt;dsh（DeepSeek Harness）的 Web UI 用一条命令就能起：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;npx @deepseek-ai&amp;#x2F;dsh --profile web
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;默认监听 &lt;code&gt;127.0.0.1:3080&lt;&#x2F;code&gt;，只有本机能访问。想实现三个目标：&lt;strong&gt;局域网设备（手机&#x2F;其他电脑）能访问&lt;&#x2F;strong&gt;、&lt;strong&gt;有用户名密码认证&lt;&#x2F;strong&gt;、&lt;strong&gt;传输加密&lt;&#x2F;strong&gt;（basicauth 明文走 HTTP 等于裸奔）。&lt;&#x2F;p&gt;
&lt;p&gt;方案：本机 caddy 反代，&lt;code&gt;tls internal&lt;&#x2F;code&gt; 内部证书 + &lt;code&gt;basicauth&lt;&#x2F;code&gt;。最终链路：&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;浏览器(局域网设备) ──https──&amp;gt; caddy:3080 (tls internal + basicauth) ──&amp;gt; 127.0.0.1:3081 (dsh web)
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;这篇文章记录完整配置和一路踩过的坑。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;yi-duan-kou-gui-hua&quot;&gt;一、端口规划&lt;&#x2F;h2&gt;
&lt;p&gt;caddy 要占 3080，dsh web 就得让位。用 &lt;code&gt;--port&lt;&#x2F;code&gt; 挪到 3081：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;npx @deepseek-ai&amp;#x2F;dsh --profile web --host 127.0.0.1 --port 3081 --no-open
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;用一个 systemd-run 临时服务包起来（&lt;code&gt;dotfiles&#x2F;.my_local&#x2F;bin&#x2F;dsh-web.sh&lt;&#x2F;code&gt;，start&#x2F;stop&#x2F;restart&#x2F;status&#x2F;log 子命令）：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;systemd-run --user --unit=dsh-web --collect \
    --working-directory=&amp;quot;$HOME&amp;quot; \
    --setenv=PATH=&amp;quot;$PATH&amp;quot; \
    --property=Restart=on-failure --property=RestartSec=2 \
    -- &amp;quot;$(command -v npx)&amp;quot; -y @deepseek-ai&amp;#x2F;dsh --profile web \
    --host 127.0.0.1 --port 3081 --no-open \
    --trusted-host 127.0.0.1:3080 --trusted-host localhost:3080 \
    --trusted-host 192.168.5.68:3080   # 局域网 IP，见第四节
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;code&gt;--trusted-host&lt;&#x2F;code&gt; 是 dsh 的 &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 浏览器信任围栏配置，后面单独讲。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;er-caddy-pei-zhi-debian-ban-cai-keng-zui-duo&quot;&gt;二、caddy 配置（Debian 版踩坑最多）&lt;&#x2F;h2&gt;
&lt;p&gt;系统：Debian 13，&lt;code&gt;apt install caddy&lt;&#x2F;code&gt;（版本 2.6.2）。Caddyfile：&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;https:&amp;#x2F;&amp;#x2F;127.0.0.1:3080, https:&amp;#x2F;&amp;#x2F;localhost:3080, https:&amp;#x2F;&amp;#x2F;192.168.5.68:3080 {
	tls internal

	basicauth {
		peter $2a$14$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
	}

	reverse_proxy 127.0.0.1:3081
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;密码哈希用 &lt;code&gt;caddy hash-password --plaintext &#x27;你的密码&#x27;&lt;&#x2F;code&gt; 生成。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;keng-1-zhi-ling-ming-shi-basicauth-bu-shi-basic-auth&quot;&gt;坑 1：指令名是 &lt;code&gt;basicauth&lt;&#x2F;code&gt; 不是 &lt;code&gt;basic_auth&lt;&#x2F;code&gt;&lt;&#x2F;h3&gt;
&lt;p&gt;上游 caddy 2.9 才把 &lt;code&gt;basic_auth&lt;&#x2F;code&gt; 改名为 &lt;code&gt;basicauth&lt;&#x2F;code&gt;，但 Debian 打包的 2.6.2 已经把改名打了进去。用老名字直接报 &lt;code&gt;unrecognized directive: basic_auth&lt;&#x2F;code&gt;。二进制里明明有模块、其他指令都正常，唯独认证指令对不上——排查时一度很迷惑。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;keng-2-zhan-dian-di-zhi-bi-xu-xie-https-host-port-wan-zheng-xing-shi&quot;&gt;坑 2：站点地址必须写 &lt;code&gt;https:&#x2F;&#x2F;host:port&lt;&#x2F;code&gt; 完整形式&lt;&#x2F;h3&gt;
&lt;p&gt;这是最隐蔽的坑。写 &lt;code&gt;:3080 { tls internal }&lt;&#x2F;code&gt;（裸端口 + tls 指令）时配置校验能过，但&lt;strong&gt;自动 HTTPS 不会为它签发任何证书&lt;&#x2F;strong&gt;——握手直接 &lt;code&gt;tlsv1 alert internal error&lt;&#x2F;code&gt;，浏览器报 &lt;code&gt;ERR_SSL_PROTOCOL_ERROR&lt;&#x2F;code&gt;。&lt;&#x2F;p&gt;
&lt;p&gt;原因：caddy 的自动证书管理只覆盖「站点地址里写了主机名&#x2F;IP 的站点」。裸端口站点没有标识符，自动 HTTPS 无从下手。改成 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt; 这种带 scheme + 地址的形式，caddy 就会为每个地址签发内部证书。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;keng-3-nei-bu-gen-zheng-shu-zhuang-bu-jin-xi-tong-xin-ren-ku&quot;&gt;坑 3：内部根证书装不进系统信任库&lt;&#x2F;h3&gt;
&lt;p&gt;caddy 服务以 &lt;code&gt;caddy&lt;&#x2F;code&gt; 用户运行，首次启动想自动把内部根证书装进系统信任库，但该用户不在 sudoers，静默失败：&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;pki.ca.local: failed to install root certificate: failed to execute sudo: exit status 1
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;手动装（注意 HOME 必须指向 caddy 用户的存储目录，否则会用 root 的身份另建一套 CA）：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;sudo env HOME=&amp;#x2F;var&amp;#x2F;lib&amp;#x2F;caddy caddy trust
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;装完后本机 &lt;code&gt;curl https:&#x2F;&#x2F;127.0.0.1:3080&#x2F;&lt;&#x2F;code&gt; 无需 &lt;code&gt;-k&lt;&#x2F;code&gt;，浏览器也不报证书错误。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;san-wei-shen-me-bi-xu-pei-trusted-host&quot;&gt;三、为什么必须配 &lt;code&gt;--trusted-host&lt;&#x2F;code&gt;&lt;&#x2F;h2&gt;
&lt;p&gt;dsh web 的 &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 是特权接口（能驱动 agent 干活），它自身没有认证，只绑 127.0.0.1——这防得住远程，但防不住本机恶意网页的跨站请求（CSRF&#x2F;驱动式攻击连 127.0.0.1 不需要任何权限）。所以 dsh 加了「浏览器信任围栏」：校验浏览器请求的 &lt;strong&gt;Origin&lt;&#x2F;strong&gt; 头，只信任自己监听地址 &lt;code&gt;127.0.0.1:3081&lt;&#x2F;code&gt;。&lt;&#x2F;p&gt;
&lt;p&gt;关键点：&lt;strong&gt;Origin 是浏览器按地址栏 URL 生成的，反向代理不会、也不该改写它&lt;&#x2F;strong&gt;。所以经过 caddy 后：&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;直连:   浏览器访问 http:&amp;#x2F;&amp;#x2F;127.0.0.1:3081   → Origin: http:&amp;#x2F;&amp;#x2F;127.0.0.1:3081   ← 与 dsh 一致，放行
经反代: 浏览器访问 https:&amp;#x2F;&amp;#x2F;127.0.0.1:3080  → Origin: https:&amp;#x2F;&amp;#x2F;127.0.0.1:3080  ← 不一致，403
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;dsh 从 TCP 视角看不出区别（请求都来自本机），但围栏只认 Origin。实测证据：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;请求&lt;&#x2F;th&gt;&lt;th&gt;结果&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;直连 3081，无 Origin（curl&#x2F;非浏览器）&lt;&#x2F;td&gt;&lt;td&gt;放行（围栏只管浏览器请求）&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;直连 3081，Origin=&lt;code&gt;127.0.0.1:3080&lt;&#x2F;code&gt;（未放行）&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;403&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;经 caddy，Origin=&lt;code&gt;127.0.0.1:3080&lt;&#x2F;code&gt;（已放行）&lt;&#x2F;td&gt;&lt;td&gt;放行&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;经 caddy，Origin=&lt;code&gt;evil.example&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;403&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;&lt;code&gt;--trusted-host 127.0.0.1:3080 --trusted-host localhost:3080&lt;&#x2F;code&gt; 就是把这些「反代入口地址」加进白名单。症状也很有迷惑性：UI 能打开、页面渲染正常，但所有 &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 请求 403，功能全废。&lt;&#x2F;p&gt;
&lt;p&gt;顺带一提：恶意网页请求 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt; 时浏览器会自动带上缓存的基本认证凭据，caddy 那关拦不住它——围栏是最后一道防线（origin 不对直接 403）。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;ding-wei-zhe-shi-csrf-fang-hu-bu-shi-cors&quot;&gt;定位：这是 CSRF 防护，不是 CORS&lt;&#x2F;h3&gt;
&lt;p&gt;容易混淆，「校验 Origin」和「CORS 配置」是两回事，区别在浏览器管什么：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;&lt;&#x2F;th&gt;&lt;th&gt;管什么&lt;&#x2F;th&gt;&lt;th&gt;谁执行&lt;&#x2F;th&gt;&lt;th&gt;dsh 案例&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;&lt;strong&gt;CORS&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td&gt;响应可不可读&lt;&#x2F;td&gt;&lt;td&gt;浏览器（看服务器回不回的 &lt;code&gt;Access-Control-Allow-*&lt;&#x2F;code&gt; 头）&lt;&#x2F;td&gt;&lt;td&gt;恶意页面 fetch &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 即使被 CORS 拒，&lt;strong&gt;请求本身也已在服务器执行&lt;&#x2F;strong&gt;，只是读不到响应&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;strong&gt;CSRF 防护（dsh 的 Origin 围栏）&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td&gt;请求该不该被执行&lt;&#x2F;td&gt;&lt;td&gt;服务端（校验请求自带的 Origin 头）&lt;&#x2F;td&gt;&lt;td&gt;请求到 dsh，Origin 不在白名单 → 403，不执行&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;CORS 通常用于「开放 API 给第三方域」场景，方向相反；想拦住&lt;strong&gt;执行&lt;&#x2F;strong&gt;只能靠服务端校验。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;wei-shen-me-cors-lan-bu-zhu-csrf-yu-jian-zhi-dui-yi-lei-qing-qiu-sheng-xiao&quot;&gt;为什么 CORS 拦不住 CSRF：预检只对一类请求生效&lt;&#x2F;h3&gt;
&lt;p&gt;浏览器 fetch 分两类：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;请求&lt;&#x2F;th&gt;&lt;th&gt;是否预检&lt;&#x2F;th&gt;&lt;th&gt;CORS 拦得住吗&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;&lt;strong&gt;简单请求&lt;&#x2F;strong&gt;：GET&#x2F;POST、content-type 只有 &lt;code&gt;form-urlencoded&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;multipart&lt;&#x2F;code&gt; &#x2F; &lt;code&gt;text&#x2F;plain&lt;&#x2F;code&gt;、无自定义头&lt;&#x2F;td&gt;&lt;td&gt;不预检，直接发出&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;拦不住&lt;&#x2F;strong&gt;——请求在服务器执行，只是 JS 读不到响应（SOP）&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;strong&gt;非简单请求&lt;&#x2F;strong&gt;：&lt;code&gt;application&#x2F;json&lt;&#x2F;code&gt;、自定义头、&lt;code&gt;Authorization&lt;&#x2F;code&gt; 等&lt;&#x2F;td&gt;&lt;td&gt;先发 OPTIONS 预检&lt;&#x2F;td&gt;&lt;td&gt;拦截：服务器没回 CORS 头，浏览器连正式请求都不发，也读不到&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;所以「随便 fetch application&#x2F;json 打任意网站」被预检拦死，但「用 &lt;code&gt;&amp;lt;form&amp;gt;&lt;&#x2F;code&gt; 或 &lt;code&gt;text&#x2F;plain&lt;&#x2F;code&gt; 的 POST&#x2F;GET」畅通无阻——这正是经典 CSRF 的载体，&lt;strong&gt;必须服务端自己判断&lt;&#x2F;strong&gt;。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;san-chong-zhen-shi-gong-ji-wei-lan-ru-he-lan-jie&quot;&gt;三种真实攻击：围栏如何拦截&lt;&#x2F;h3&gt;
&lt;p&gt;&lt;strong&gt;攻击 A：表单 CSRF（经典形态，不需要点击）&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;恶意网页放一个隐藏表单 + 一段自动提交脚本——用户打开页面即发起请求，无感知、无需点击：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;html&quot; class=&quot;language-html &quot;&gt;&lt;code class=&quot;language-html&quot; data-lang=&quot;html&quot;&gt;&amp;lt;form id=&amp;quot;evil&amp;quot; method=&amp;quot;POST&amp;quot; action=&amp;quot;https:&amp;#x2F;&amp;#x2F;127.0.0.1:3080&amp;#x2F;api&amp;#x2F;run-agent&amp;quot;&amp;gt;
  &amp;lt;input name=&amp;quot;prompt&amp;quot; value=&amp;quot;rm -rf ~ &amp;amp;&amp;amp; ...&amp;quot;&amp;gt;
&amp;lt;&amp;#x2F;form&amp;gt;
&amp;lt;script&amp;gt;
  document.getElementById(&amp;#x27;evil&amp;#x27;).submit()   &amp;#x2F;&amp;#x2F; JS 自动提交，不依赖用户点击
&amp;lt;&amp;#x2F;script&amp;gt;
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;表单的跨站提交不受 CORS&#x2F;SOP 限制（CORS 只管 fetch&#x2F;XHR 的响应可读性），浏览器会带上 Caddy 缓存的 Basic 凭据。&lt;strong&gt;无围栏&lt;&#x2F;strong&gt;：dsh 执行，&lt;code&gt;rm -rf&lt;&#x2F;code&gt; 跑起来。&lt;strong&gt;有围栏&lt;&#x2F;strong&gt;：浏览器自动带的 &lt;code&gt;Origin: https:&#x2F;&#x2F;evil.example&lt;&#x2F;code&gt; 不在白名单 → 403。&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;攻击 B：&lt;code&gt;text&#x2F;plain&lt;&#x2F;code&gt; 伪 JSON&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;攻击者知道后端收 JSON，但 fetch 用 &lt;code&gt;text&#x2F;plain&lt;&#x2F;code&gt; 发 &lt;code&gt;JSON.stringify&lt;&#x2F;code&gt; 的 body 绕过预检——很多 API 只看内容不看 content-type，照样执行。围栏同样以 Origin 一票否决。&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;攻击 C：DNS rebinding（最隐蔽）&lt;&#x2F;strong&gt;&lt;&#x2F;p&gt;
&lt;p&gt;攻击者域名 &lt;code&gt;evil.com&lt;&#x2F;code&gt; 先解析到外网，JS 建立连接后再解析到 &lt;code&gt;127.0.0.1&lt;&#x2F;code&gt;——此时 fetch &lt;code&gt;https:&#x2F;&#x2F;evil.com&#x2F;api&#x2F;...&lt;&#x2F;code&gt; 在浏览器眼里是&lt;strong&gt;同源请求&lt;&#x2F;strong&gt;：不预检、响应可读、直连 dsh。但 Origin 头仍是 &lt;code&gt;https:&#x2F;&#x2F;evil.com&lt;&#x2F;code&gt;，围栏 403。没有服务端 Origin 校验的话，这个攻击连 CORS 都拦不住（它根本不同源）。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;wei-shen-me-caddy-de-basicauth-dang-bu-zhu-wei-lan-shi-zui-hou-yi-dao&quot;&gt;为什么 caddy 的 basicauth 挡不住、围栏是最后一道&lt;&#x2F;h3&gt;
&lt;p&gt;攻击者的请求确实会带着&lt;strong&gt;你浏览器缓存的 Basic 凭据&lt;&#x2F;strong&gt;（你访问 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt; 时输过的密码，浏览器记忆后会自动附上 &lt;code&gt;Authorization&lt;&#x2F;code&gt; 头），caddy 认证天然放行——caddy 认不出「这是恶意页面发的，用户没点」。所以防线落在 dsh：Origin 头永远只有地址栏来源意义上的值，&lt;strong&gt;攻击者无法伪造页面来源&lt;&#x2F;strong&gt;，他永远过不了这关（无 Origin 的请求 dsh 视为非浏览器、放行，但恶意网页的跨站请求一定带 Origin）。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;wei-shen-me-pu-tong-wang-zhan-bu-kao-origin-xiao-yan-dang-zhu-fang-xian&quot;&gt;为什么普通网站不靠 Origin 校验当主防线&lt;&#x2F;h3&gt;
&lt;p&gt;Origin&#x2F;Referer 校验是少量站点会用的补充手段（GitHub 曾用「Origin 校验 + token 兜底」），但主流主防线是：&lt;&#x2F;p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;SameSite Cookie&lt;&#x2F;strong&gt;：浏览器默认 &lt;code&gt;SameSite=Lax&lt;&#x2F;code&gt;（2020 年 Chromium 默认），恶意页面发起的跨站 POST 连登录 Cookie 都不带，CSRF 自动失效——大多数站点就靠这一条&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;CSRF token&lt;&#x2F;strong&gt;：表单嵌服务端签发的 token，提交校验&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;CORS 配置&lt;&#x2F;strong&gt;：管读取不挡执行，&lt;code&gt;Access-Control-Allow-Origin: *&lt;&#x2F;code&gt; 的站一抓一大把，从不是 CSRF 防线&lt;&#x2F;li&gt;
&lt;&#x2F;ol&gt;
&lt;p&gt;&lt;strong&gt;dsh 是特殊形态&lt;&#x2F;strong&gt;：无认证、无 Cookie、无会话的本地特权 API——SameSite 没 Cookie 可保护，token 没有登录仪式可签发，只剩 Origin 头本身可校验。所以「裸 API + 本机监听」才不得不用 Origin 围栏。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;si-ju-yu-wang-ip-zi-dong-shou-ji&quot;&gt;四、局域网 IP 自动收集&lt;&#x2F;h2&gt;
&lt;p&gt;caddy 只给站点地址列表里的 IP 签发证书，DHCP 换 IP 就断。我让 install 脚本渲染时自动收集 &lt;code&gt;hostname -I&lt;&#x2F;code&gt; 的全部非回环 IPv4（局域网&#x2F;tailscale&#x2F;docker bridge），拼进站点地址：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;# 渲染 Caddyfile 时
for h in $(hostname -I | tr &amp;#x27; &amp;#x27; &amp;#x27;\n&amp;#x27; | grep -E &amp;#x27;^[0-9.]+$&amp;#x27; | grep -v &amp;#x27;^127\.&amp;#x27;); do
    SITES=&amp;quot;$SITES, https:&amp;#x2F;&amp;#x2F;$h:3080&amp;quot;
done
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;dsh-web.sh 启动时做同样的事，把每个 IP 都加进 &lt;code&gt;--trusted-host&lt;&#x2F;code&gt;——否则通过局域网 IP 访问时 &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 照样 403。换 IP 后重跑安装脚本 + 重启 dsh web 即可。&lt;&#x2F;p&gt;
&lt;p&gt;完整实现（模板 + 幂等安装脚本 + 服务脚本）在工具仓库：&lt;code&gt;homelab&#x2F;caddy&#x2F;&lt;&#x2F;code&gt;（Caddyfile.tmpl、install.sh，密钥不入库）和 &lt;code&gt;dotfiles&#x2F;.my_local&#x2F;bin&#x2F;dsh-web.sh&lt;&#x2F;code&gt;。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;wu-ju-yu-wang-qi-ta-she-bei-fang-wen&quot;&gt;五、局域网其他设备访问&lt;&#x2F;h2&gt;
&lt;p&gt;本机浏览器直接可用（根证书已装系统信任库）。&lt;strong&gt;其他设备&lt;&#x2F;strong&gt;（手机&#x2F;别的电脑）需要先信任 caddy 的根证书，否则握手成功但报 &lt;code&gt;ERR_CERT_AUTHORITY_INVALID&lt;&#x2F;code&gt;：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;# 本机把根证书复制出来，起个临时 HTTP 服务供设备下载
sudo cp &amp;#x2F;usr&amp;#x2F;local&amp;#x2F;share&amp;#x2F;ca-certificates&amp;#x2F;Caddy_Local_Authority_*.crt &amp;#x2F;tmp&amp;#x2F;caddy-root.crt
python3 -m http.server 8000 -d &amp;#x2F;tmp
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;设备浏览器访问 &lt;code&gt;http:&#x2F;&#x2F;192.168.5.68:8000&#x2F;caddy-root.crt&lt;&#x2F;code&gt; 下载并安装：&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Android&lt;&#x2F;strong&gt;：设置 → 安全 → 安装 CA 证书（网络与互联网 → 加密与凭据）&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;iOS&lt;&#x2F;strong&gt;：下载 → 设置 → 已下载描述文件 → 安装，再在「证书信任设置」开启完全信任&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;桌面&lt;&#x2F;strong&gt;：导入系统信任库（Debian&#x2F;Ubuntu：拷到 &lt;code&gt;&#x2F;usr&#x2F;local&#x2F;share&#x2F;ca-certificates&#x2F;&lt;&#x2F;code&gt; 后 &lt;code&gt;update-ca-certificates&lt;&#x2F;code&gt;）&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;装完后访问 &lt;code&gt;https:&#x2F;&#x2F;192.168.5.68:3080&lt;&#x2F;code&gt;，输入用户名密码即可。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;liu-mo-xing-pei-zhi-wei-shen-me-bi-xu-cong-127-0-0-1-fang-wen&quot;&gt;六、模型配置为什么必须从 127.0.0.1 访问&lt;&#x2F;h2&gt;
&lt;p&gt;第五节做完后，UI 打开正常、聊天正常，但&lt;strong&gt;点开「模型配置」页却报错&lt;&#x2F;strong&gt;：&lt;&#x2F;p&gt;
&lt;pre&gt;&lt;code&gt;加载提供方目录失败: settings are unavailable in this browser
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;&lt;code&gt;--trusted-host 192.168.5.68:3080&lt;&#x2F;code&gt; 明明加了（见第一节），为什么 &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 还是不放行？——因为它没坏，&lt;strong&gt;这是 dsh 0.1.1-rc.2 起的设计&lt;&#x2F;strong&gt;：&lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 被分成两类方法：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;方法类&lt;&#x2F;th&gt;&lt;th&gt;例子&lt;&#x2F;th&gt;&lt;th&gt;围栏白名单&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;普通&lt;&#x2F;td&gt;&lt;td&gt;会话创建、&lt;code&gt;llm.providers&lt;&#x2F;code&gt; 目录、聊天&lt;&#x2F;td&gt;&lt;td&gt;&lt;code&gt;trustedHosts&lt;&#x2F;code&gt;（&lt;code&gt;--trusted-host&lt;&#x2F;code&gt;）全部生效&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;strong&gt;配置平面&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;td&gt;&lt;code&gt;settings.describe&#x2F;update&#x2F;replace&#x2F;mutate&lt;&#x2F;code&gt;、&lt;code&gt;credentials.*&lt;&#x2F;code&gt;、&lt;code&gt;agentPreset.*&lt;&#x2F;code&gt;、&lt;code&gt;host.*&lt;&#x2F;code&gt;、&lt;code&gt;llm.discoverModels&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;固定传「空列表」——只有 loopback 来源才放行&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;设计文档原话（大意）：&lt;code&gt;trustedHosts&lt;&#x2F;code&gt; 只是 DNS-rebinding 围栏，&lt;strong&gt;不是认证层&lt;&#x2F;strong&gt;；配置平面（读也不行——&lt;code&gt;settings.describe&lt;&#x2F;code&gt; 会原样返回全部配置和密钥状态）在真正的认证层出现前，永远保持 loopback-same-origin。&lt;code&gt;--host 0.0.0.0&lt;&#x2F;code&gt; 也因此被官方禁用。&lt;&#x2F;p&gt;
&lt;h3 id=&quot;wei-shen-me-fan-dai-liao-huan-shi-bu-xing-dsh-bu-kan-tcp-lai-yuan-zhi-kan-host-tou&quot;&gt;为什么反代了还是不行：dsh 不看 TCP 来源，只看 Host 头&lt;&#x2F;h3&gt;
&lt;p&gt;反代后 dsh 收到的连接确实来自 127.0.0.1（caddy 转发），但 dsh &lt;strong&gt;故意不信任 TCP 回环&lt;&#x2F;strong&gt;——因为 DNS rebinding 攻击的经典形态就是「socket 打到 loopback、Host 却是攻击者域名」，回环连接完全不能证明来源可信。而 caddy 的 &lt;code&gt;reverse_proxy&lt;&#x2F;code&gt; 默认把客户端原始 Host 原样透传（&lt;code&gt;192.168.5.68:3080&lt;&#x2F;code&gt;），dsh 看到的权威是非回环 → 配置平面 403。&lt;&#x2F;p&gt;
&lt;p&gt;实测证据（直接打 3081，模拟反代过来的请求）：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;# Host=局域网 IP，Origin=局域网 IP → 配置平面 403
curl -s -X POST -o &amp;#x2F;dev&amp;#x2F;null -w &amp;#x27;%{http_code}\n&amp;#x27; \
  -H &amp;#x27;Host: 192.168.5.68:3080&amp;#x27; -H &amp;#x27;Origin: https:&amp;#x2F;&amp;#x2F;192.168.5.68:3080&amp;#x27; \
  http:&amp;#x2F;&amp;#x2F;127.0.0.1:3081&amp;#x2F;api&amp;#x2F;settings.describe
# → 403

# 同一 TCP 回环、Host 换回 127.0.0.1 → 通过围栏（后面的 415 只是没带 content-type）
curl -s -X POST -o &amp;#x2F;dev&amp;#x2F;null -w &amp;#x27;%{http_code}\n&amp;#x27; \
  -H &amp;#x27;Host: 127.0.0.1:3081&amp;#x27; -H &amp;#x27;Origin: https:&amp;#x2F;&amp;#x2F;127.0.0.1:3081&amp;#x27; \
  http:&amp;#x2F;&amp;#x2F;127.0.0.1:3081&amp;#x2F;api&amp;#x2F;settings.describe
# → 415
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h3 id=&quot;jie-fa-rang-liu-lan-qi-origin-hui-dao-127-0-0-1&quot;&gt;解法：让浏览器 origin 回到 127.0.0.1&lt;&#x2F;h3&gt;
&lt;p&gt;两个方案，都不需要改 dsh&#x2F;caddy 上游：&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;方案 A（最推荐）：本机配一次，全局域网共享。&lt;&#x2F;strong&gt; 在本机访问 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt; 配置模型&#x2F;凭证——配置存进 dsh 的共享配置文件，其他设备的浏览器 UI 读同一份，聊天照常。大多数场景到这里就结束了。&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;方案 B（其他设备上也要配模型）：SSH 隧道，&lt;code&gt;dotfiles&#x2F;.my_local&#x2F;bin&#x2F;dsh-proxy.sh&lt;&#x2F;code&gt;。&lt;&#x2F;strong&gt; 客户端起一条隧道，把远端 &lt;code&gt;127.0.0.1:3080&lt;&#x2F;code&gt;（caddy）转发成本地 &lt;code&gt;127.0.0.1:3080&lt;&#x2F;code&gt;，浏览器地址栏仍是回环，origin 回到 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt;，一切放行（caddy basicauth 依然生效）：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;# 客户端（任何 Linux，systemd --user + ssh + iproute2）
dsh-proxy.sh start                 # 默认 peter@192.168.5.68，可用 $DSH_REMOTE &amp;#x2F; 参数覆盖
# 然后本机浏览器开 https:&amp;#x2F;&amp;#x2F;127.0.0.1:3080

dsh-proxy.sh [stop|restart|status|log]
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;内部实现就是一个 systemd-run --user 包起来的 ssh：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;systemd-run --user --unit=dsh-proxy --collect \
    --setenv=GPG_TTY=&amp;#x2F;dev&amp;#x2F;nonexistent \
    --property=Restart=on-failure --property=RestartSec=5 \
    -- ssh -N -L 3080:127.0.0.1:3080 \
    -o ExitOnForwardFailure=yes -o StrictHostKeyChecking=accept-new \
    -o ServerAliveInterval=30 -o ServerAliveCountMax=3 \
    peter@192.168.5.68
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;几个细节：&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;GPG_TTY=&#x2F;dev&#x2F;nonexistent&lt;&#x2F;code&gt;：ssh 可能触发 gpg-agent 口令提示，隧道环境没有终端，pinentry 会挂起无输出；失效时让它快速失败（报 &lt;code&gt;agent refused operation&lt;&#x2F;code&gt;），自己在客户端终端解锁一次 ssh 密钥即可&lt;&#x2F;li&gt;
&lt;li&gt;&lt;code&gt;ExitOnForwardFailure&lt;&#x2F;code&gt;：本地端口被占（如本机也在跑 caddy 3080）直接失败退出，不静默挂起&lt;&#x2F;li&gt;
&lt;li&gt;&lt;code&gt;ServerAliveInterval + Restart=on-failure&lt;&#x2F;code&gt;：网络断了 ssh 快速退出（exit 255），systemd 自动重连&lt;&#x2F;li&gt;
&lt;li&gt;&lt;code&gt;StrictHostKeyChecking=accept-new&lt;&#x2F;code&gt;：首次连接自动接受 host key，居家环境免交互&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;h3 id=&quot;wei-shen-me-bu-gan-cui-rang-caddy-gai-xie-host&quot;&gt;为什么不干脆让 caddy 改写 Host&lt;&#x2F;h3&gt;
&lt;p&gt;加 &lt;code&gt;header_up Host 127.0.0.1:3081&lt;&#x2F;code&gt; 确实能骗过 Host 检查，但浏览器对同源 &lt;code&gt;fetch&lt;&#x2F;code&gt; 的 &lt;strong&gt;POST 会带 &lt;code&gt;Origin: https:&#x2F;&#x2F;192.168.5.68:3080&lt;&#x2F;code&gt;&lt;&#x2F;strong&gt;，Host 换成 &lt;code&gt;127.0.0.1:3081&lt;&#x2F;code&gt; 后 Origin 与 Host 不匹配 → 依旧 403。再 &lt;code&gt;header_up -Origin&lt;&#x2F;code&gt; 删掉 Origin 倒能过，但这就等于把 dsh 的配置平面围栏&lt;strong&gt;整体拆除&lt;&#x2F;strong&gt;——服务器对「任意来源的配置平面请求」放行，只剩 basicauth 一层（而 basicauth 本就挡不住浏览器自动附带的缓存凭据，参考第三节）。dsh 官方把这标为设计特性等着「真正的认证层」出现，不建议硬拆。&lt;&#x2F;p&gt;
&lt;h2 id=&quot;qi-rong-qi-chang-jing-jian-ting-suo-you-di-zhi-zheng-shu-dao-di-fa-gei-shui&quot;&gt;七、容器场景：监听「所有地址」，证书到底发给谁？&lt;&#x2F;h2&gt;
&lt;p&gt;本节实测（caddy:2.10 容器 + &lt;code&gt;-p 8000:8000&lt;&#x2F;code&gt;）。结论一句话：&lt;strong&gt;证书发给「站点地址」，与监听地址无关&lt;&#x2F;strong&gt;。caddy 对同一端口自动以通配地址监听（&lt;code&gt;[::]:8000&lt;&#x2F;code&gt;，也就是 0.0.0.0 的效果），但证书 SAN 只按你写进站点地址列表的标识符签发。所以「监听 0.0.0.0 证书是不是还能正确」答案取决于你写了什么：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;站点地址&lt;&#x2F;th&gt;&lt;th&gt;结果（实测）&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;:8000&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;不签发任何证书，握手直接 &lt;code&gt;internal error&lt;&#x2F;code&gt;（同坑 2，2.6&#x2F;2.10 行为一致）&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;https:&#x2F;&#x2F;0.0.0.0:8000&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;危险&lt;&#x2F;strong&gt;：caddy 真的会签发证书，但 SAN 是字面 &lt;code&gt;IP Address:0.0.0.0&lt;&#x2F;code&gt;，真实 IP 访问全部验证失败&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;https:&#x2F;&#x2F;&amp;lt;容器IP&amp;gt;:8000&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;SAN=容器 IP（如 172.17.0.2），签出来的是「容器内地址的证书」；宿主机&#x2F;局域网用户访问用的是宿主 IP，至少 SNI 对不上、容器重建 IP 还变，&lt;strong&gt;永远用不上&lt;&#x2F;strong&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;&lt;code&gt;https:&#x2F;&#x2F;&amp;lt;宿主LAN IP&amp;gt;:8000&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;SAN=宿主 LAN IP（正确），但还差最后一公里，见下&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;&lt;strong&gt;为什么 &lt;code&gt;https:&#x2F;&#x2F;IP&lt;&#x2F;code&gt; 这种访问会失败——SNI 才是领证书的钥匙。&lt;&#x2F;strong&gt; 证书按站点地址签发，但 TLS 握手时客户端要发一个 SNI（Server Name Indication）告诉服务端「我要哪张证书」。实测各客户端对 IP 地址的行为并不一致：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;客户端&lt;&#x2F;th&gt;&lt;th&gt;访问 &lt;code&gt;https:&#x2F;&#x2F;IP:8000&lt;&#x2F;code&gt; 时发 SNI 吗&lt;&#x2F;th&gt;&lt;th&gt;无 &lt;code&gt;default_sni&lt;&#x2F;code&gt; 时结果&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;Chromium 系（Chrome&#x2F;Edge&#x2F;Android）&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;不发&lt;&#x2F;strong&gt;（对域名才发）&lt;&#x2F;td&gt;&lt;td&gt;&lt;code&gt;ERR_SSL_PROTOCOL_ERROR&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;curl&lt;&#x2F;td&gt;&lt;td&gt;&lt;strong&gt;不发&lt;&#x2F;strong&gt;（对域名才发）&lt;&#x2F;td&gt;&lt;td&gt;000（TLS internal error），和 Chrome 站一边，最容易误导排查&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;openssl s_client&lt;&#x2F;td&gt;&lt;td&gt;默认不发；&lt;code&gt;-servername&lt;&#x2F;code&gt; 显式发&lt;&#x2F;td&gt;&lt;td&gt;不发 SNI 会被拒；显式 &lt;code&gt;-servername IP&lt;&#x2F;code&gt; 能拿到证书&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;caddy 2.10 对无 SNI 的 ClientHello 直接拒绝；&lt;strong&gt;老版 2.6.2 反而有兜底&lt;&#x2F;strong&gt;（发给首域证书——本机浏览器直接访问 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt; 能工作正是靠它），2.7 引入 &lt;code&gt;default_sni&lt;&#x2F;code&gt; 后移除了这个兜底，所以容器版必须显式配置。&lt;&#x2F;p&gt;
&lt;p&gt;解法：全局 &lt;code&gt;default_sni&lt;&#x2F;code&gt; 指定无 SNI 时该给哪张证书：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;caddyfile&quot; class=&quot;language-caddyfile &quot;&gt;&lt;code class=&quot;language-caddyfile&quot; data-lang=&quot;caddyfile&quot;&gt;{
	default_sni 192.168.5.68
}

https:&amp;#x2F;&amp;#x2F;127.0.0.1:8000, https:&amp;#x2F;&amp;#x2F;localhost:8000, https:&amp;#x2F;&amp;#x2F;192.168.5.68:8000 {
	tls internal
	basicauth {
		peter $2a$14$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
	}
	reverse_proxy 127.0.0.1:3081
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;加 &lt;code&gt;default_sni&lt;&#x2F;code&gt; 前后对照（同样配置、同一容器）：&lt;&#x2F;p&gt;
&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;客户端&lt;&#x2F;th&gt;&lt;th&gt;无 &lt;code&gt;default_sni&lt;&#x2F;code&gt;&lt;&#x2F;th&gt;&lt;th&gt;有 &lt;code&gt;default_sni&lt;&#x2F;code&gt;&lt;&#x2F;th&gt;&lt;&#x2F;tr&gt;&lt;&#x2F;thead&gt;&lt;tbody&gt;
&lt;tr&gt;&lt;td&gt;curl（无 SNI）&lt;&#x2F;td&gt;&lt;td&gt;000 &#x2F; internal error&lt;&#x2F;td&gt;&lt;td&gt;200，证书 SAN 正确&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;tr&gt;&lt;td&gt;chromium 打开 IP URL&lt;&#x2F;td&gt;&lt;td&gt;&lt;code&gt;ERR_SSL_PROTOCOL_ERROR&lt;&#x2F;code&gt;&lt;&#x2F;td&gt;&lt;td&gt;&lt;code&gt;ERR_CERT_AUTHORITY_INVALID&lt;&#x2F;code&gt;（握手已成功，只差 CA 信任）&lt;&#x2F;td&gt;&lt;&#x2F;tr&gt;
&lt;&#x2F;tbody&gt;&lt;&#x2F;table&gt;
&lt;p&gt;&lt;code&gt;ERR_CERT_AUTHORITY_INVALID&lt;&#x2F;code&gt; 意味着&quot;证书和域名都对，只差设备信任根证书&quot;。容器里的 CA 这样导出：&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;docker cp &amp;lt;容器名&amp;gt;:&amp;#x2F;data&amp;#x2F;caddy&amp;#x2F;pki&amp;#x2F;authorities&amp;#x2F;local&amp;#x2F;root.crt .&amp;#x2F;root.crt
openssl x509 -in root.crt -noout -subject -dates
# subject=CN=Caddy Local Authority - 2026 ECC Root，有效期 10 年
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;（容器内证书以容器用户写入，卷目录在宿主机侧常是 uid 1000&#x2F;root 权限，直接 &lt;code&gt;ls&lt;&#x2F;code&gt; 可能没权限，用 &lt;code&gt;docker cp&lt;&#x2F;code&gt; 最省事；挂卷前 &lt;code&gt;mkdir -p .&#x2F;data &amp;amp;&amp;amp; sudo chown 1000:1000 .&#x2F;data&lt;&#x2F;code&gt; 也能解决。）&lt;&#x2F;p&gt;
&lt;p&gt;容器场景几个经验：&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;别拿容器 IP 当站点地址&lt;&#x2F;strong&gt;——每次重建都会变，还会让 LAN 用户 SNI 对不上。固定写宿主机 LAN IP，或配内部域名更稳（浏览器对域名一定发 SNI，证书按域名签，不受 default_sni 限制）。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;default_sni&lt;&#x2F;code&gt; 只能设一个值&lt;&#x2F;strong&gt;：LAN &#x2F; tailscale 多个 IP 里只有它指定的那个能被&quot;裸 IP 无 SNI&quot;访问；其余 IP 要么换域名，要么接受 hostname 不匹配。&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;内部证书叶证书默认只有 12h 有效期&lt;&#x2F;strong&gt;，长跑要拉长。注意 caddy 2.10 的全局块里没有 &lt;code&gt;tls&lt;&#x2F;code&gt; 选项（实测报 &lt;code&gt;unrecognized global option: tls&lt;&#x2F;code&gt;），lifetime 写在站点级：&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;pre data-lang=&quot;caddyfile&quot; class=&quot;language-caddyfile &quot;&gt;&lt;code class=&quot;language-caddyfile&quot; data-lang=&quot;caddyfile&quot;&gt;https:&amp;#x2F;&amp;#x2F;192.168.5.68:8000 {
	tls {
		issuer internal {
			lifetime 8760h   # 默认仅 12h
		}
	}
	basicauth { ... }
	reverse_proxy 127.0.0.1:3081
}
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h2 id=&quot;yan-zheng&quot;&gt;验证&lt;&#x2F;h2&gt;
&lt;pre data-lang=&quot;bash&quot; class=&quot;language-bash &quot;&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;curl -s -o &amp;#x2F;dev&amp;#x2F;null -w &amp;#x27;%{http_code}\n&amp;#x27; https:&amp;#x2F;&amp;#x2F;192.168.5.68:3080&amp;#x2F;          # 无凭据 → 401
curl -s -u peter:密码 -o &amp;#x2F;dev&amp;#x2F;null -w &amp;#x27;%{http_code}\n&amp;#x27; https:&amp;#x2F;&amp;#x2F;192.168.5.68:3080&amp;#x2F;  # → 200
curl -s -u peter:密码 -H &amp;#x27;Origin: https:&amp;#x2F;&amp;#x2F;evil.example&amp;#x27; https:&amp;#x2F;&amp;#x2F;192.168.5.68:3080&amp;#x2F;api&amp;#x2F;  # → 403
&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;h2 id=&quot;zong-jie&quot;&gt;总结&lt;&#x2F;h2&gt;
&lt;ul&gt;
&lt;li&gt;端口让位：caddy 占 3080，dsh web 挪到 3081&lt;&#x2F;li&gt;
&lt;li&gt;Debian 版 caddy 两个差异：&lt;code&gt;basicauth&lt;&#x2F;code&gt; 指令名、站点地址必须 &lt;code&gt;https:&#x2F;&#x2F;host:port&lt;&#x2F;code&gt; 完整形式&lt;&#x2F;li&gt;
&lt;li&gt;内部根证书手动信任：&lt;code&gt;sudo env HOME=&#x2F;var&#x2F;lib&#x2F;caddy caddy trust&lt;&#x2F;code&gt;&lt;&#x2F;li&gt;
&lt;li&gt;dsh 的 &lt;code&gt;&#x2F;api&lt;&#x2F;code&gt; 围栏校验浏览器 Origin（服务端 CSRF 防护，不是 CORS），代理不改 Origin，必须用 &lt;code&gt;--trusted-host&lt;&#x2F;code&gt; 显式放行反代入口（包括局域网 IP）；围栏也是 basicauth 拦不住的本机 CSRF 的最后防线&lt;&#x2F;li&gt;
&lt;li&gt;局域网设备装一次根证书即可永久使用，换 IP 重跑脚本重新签发&lt;&#x2F;li&gt;
&lt;li&gt;配置模型&#x2F;凭证永远走 loopback origin：本机 &lt;code&gt;https:&#x2F;&#x2F;127.0.0.1:3080&lt;&#x2F;code&gt;，或客户端 &lt;code&gt;dsh-proxy.sh&lt;&#x2F;code&gt; SSH 隧道转发；&lt;code&gt;--trusted-host&lt;&#x2F;code&gt; 帮不了配置平面（dsh ≥0.1.1-rc.2 设计如此），也别用 caddy 改写 Host&#x2F;删 Origin 硬拆围栏&lt;&#x2F;li&gt;
&lt;li&gt;容器场景：证书按「站点地址」（勿用容器 IP&#x2F;0.0.0.0），配全局 &lt;code&gt;default_sni&lt;&#x2F;code&gt; 兜底 Chromium 系的无 SNI 请求，CA 从卷目录 &lt;code&gt;&#x2F;data&#x2F;caddy&#x2F;pki&#x2F;authorities&#x2F;local&#x2F;root.crt&lt;&#x2F;code&gt; 导出&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
</content>
        
    </entry>
</feed>
